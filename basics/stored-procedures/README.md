# Stored Procedures:

A Block of SQL statements are stored in DB. They are primarily used to control the Transactions.

- It is differ from the Functions, as functions returns the value, but Procedures wont.
- Executing the stored procedurs will modify the data in DB
