# Common Table Expression:

A CTE, is a temporary named query, that was set using the `WITH` clause. Some of the properties of CTE are,

1. The name can be referenced across the same Query
2. It can store the result
3. Life span only during the time of execution

- It helps to hold the Quries.
